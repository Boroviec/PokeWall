{
  "rules": {
    "cards": {
      ".read": true,
      "$cardId": {
        ".write": "auth != null && root.child('admins').child(auth.uid).val() === true",
        ".validate": "!newData.exists() || (newData.hasChildren(['url','timestamp','sizeBytes']) && newData.child('url').isString() && newData.child('url').val().matches('^data:image/(jpeg|jpg|png);base64,.*$') && newData.child('timestamp').isNumber() && newData.child('sizeBytes').isNumber() && newData.child('sizeBytes').val() <= 5242880 && (!newData.child('width').exists() || newData.child('width').isNumber()) && (!newData.child('height').exists() || newData.child('height').isNumber()))"
      }
    },
    "admins": {
      "$uid": {
        ".read": "auth != null && auth.uid === $uid",
        ".write": false
      }
    }
  }
}
          ".validate": "(!newData.exists()) || newData.isNumber()"
        },
        "height": {
          ".validate": "(!newData.exists()) || newData.isNumber()"
        },
        "$other": {
          ".validate": false
        }
      }
    }
  }
}
